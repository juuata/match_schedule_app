<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>GOALPOST - Live Scores & Updates</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <nav class="bg-white shadow-lg">
      <div class="container mx-auto px-4">
        <div class="flex items-center justify-between h-20">
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center" do %>
              <%= image_tag "logo.png", alt: "GOALPOST", class: "h-20" %>
            <% end %>
          </div>
          <div class="flex items-center">
            <% if logged_in? %>
              <%= link_to "リーグ一覧", leagues_path, class: "text-gray-700 hover:bg-gray-200 px-4 h-20 flex items-center font-medium transition duration-200" %>
              <%= link_to "試合日程", schedule_matches_path, class: "text-gray-700 hover:bg-gray-200 px-4 h-20 flex items-center font-medium transition duration-200" %>
              <%= link_to "試合結果", results_matches_path, class: "text-gray-700 hover:bg-gray-200 px-4 h-20 flex items-center font-medium transition duration-200" %>
              <%= link_to "ライブ", live_matches_path, class: "text-gray-700 hover:bg-gray-200 px-4 h-20 flex items-center font-medium transition duration-200" %>
              <%= link_to favorites_path, class: "text-gray-700 hover:bg-gray-200 px-4 h-20 flex items-center font-medium transition duration-200" do %>
                <svg class="w-5 h-5 mr-1 text-yellow-500" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"/>
                </svg>
                お気に入り
              <% end %>

              <div class="border-l border-gray-300 h-8 mx-4"></div>

              <span class="text-gray-700 font-medium px-2">ようこそ、<%= current_user.name %>さん</span>
              <%= button_to "ログアウト", logout_path, method: :delete, class: "bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded-lg font-medium transition duration-200 ml-2" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container mx-auto mt-8 px-4">
      <% if flash[:notice] %>
        <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-6">
          <p><%= flash[:notice] %></p>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6">
          <p><%= flash[:alert] %></p>
        </div>
      <% end %>
      <% if @using_dummy_data %>
        <div class="bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700 p-4 mb-6">
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
            </svg>
            <p><strong>デモモード:</strong> サンプルデータを表示しています。実際の試合データを表示するには <code class="bg-yellow-200 px-1 rounded">.env</code> ファイルにAPIキーを設定してください。</p>
          </div>
        </div>
      <% end %>
      <%= yield %>
    </main>

    <footer class="mt-16 py-8 bg-gray-800 text-gray-400 text-center">
      <p>Powered by API-Football</p>
    </footer>
  </body>
</html>
