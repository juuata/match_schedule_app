<!DOCTYPE html>
<html>
  <head>
    <title>Football Match Schedule</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <nav class="bg-green-700 text-white shadow-lg">
      <div class="container mx-auto px-4">
        <div class="flex items-center justify-between h-16">
          <div class="flex items-center">
            <%= link_to "Football Match Schedule", root_path, class: "text-xl font-bold" %>
          </div>
          <div class="flex space-x-4">
            <%= link_to "Leagues", leagues_path, class: "hover:bg-green-600 px-3 py-2 rounded" %>
            <%= link_to "Schedule", schedule_matches_path, class: "hover:bg-green-600 px-3 py-2 rounded" %>
            <%= link_to "Results", results_matches_path, class: "hover:bg-green-600 px-3 py-2 rounded" %>
            <%= link_to "Live", live_matches_path, class: "hover:bg-green-600 px-3 py-2 rounded" %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container mx-auto mt-8 px-4">
      <% if @using_dummy_data %>
        <div class="bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700 p-4 mb-6">
          <div class="flex items-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
            </svg>
            <p><strong>Demo Mode:</strong> Displaying sample data. Set your API key in <code class="bg-yellow-200 px-1 rounded">.env</code> file to see real match data.</p>
          </div>
        </div>
      <% end %>
      <%= yield %>
    </main>

    <footer class="mt-16 py-8 bg-gray-800 text-gray-400 text-center">
      <p>Powered by API-Football</p>
    </footer>
  </body>
</html>
